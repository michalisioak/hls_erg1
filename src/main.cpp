#include "read_txt.h"
#include "top.h"
#include <iostream>

#define A_SIZE 5
#define H_ROWS 500
#define W_COLS 16

int main() {
  std::cout << "Init Testing...." << std::endl;

  //   createRandomGraph(A);
  //   computeDegreeMatrix(A, D);
  //   computeNormalizedAdjacencyMatrix(A, D, A_tilde);
  //   createFeatureMatrix(H_in);

  //   calc<int, H_ROWS, H_COLS, W_COLS>(A, H_in, H_out_dut);

  return 0;
}
